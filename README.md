# regex_search
Finds any regex pattern in files inside of given directory along with subdirectories. Then outputs file location pattern was found on, pattern matched with, line numbers in which pattern was matched on, etc. Output in different file types (csv,xlsx).  
